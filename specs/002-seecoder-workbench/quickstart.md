# 2.0 快速验证

```powershell
pnpm install --frozen-lockfile
pnpm lint
pnpm typecheck
pnpm test:unit
pnpm test:integration
pnpm test:e2e
pnpm build
pnpm dev
```

启动后选择项目，使用 Plan 模式输入任务，批准计划后切换 Guided，观察 Explore、Diff、审批、Terminal、Review 和 Checkpoint。
