# SeeCoder Spec-Driven Workflow

The project uses the Spec Kit sequence:

```text
constitution -> specify -> clarify -> plan -> checklist -> tasks -> analyze -> implement
```

The source of truth is `.specify/memory/constitution.md`; feature artifacts live under `specs/`.

