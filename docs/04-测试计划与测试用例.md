# 测试计划

## 单元测试

覆盖 SSE 碎片重组、schema 错误、状态转换、取消/上限、重复调用、上下文压缩、路径约束、Diff 回滚、权限矩阵、子 Agent 白名单和 JSONL 恢复。

## 集成测试

使用 fake SSE provider 和临时 Git 仓库覆盖：读取→修改→测试→完成、允许/拒绝审批、429 重试、命令超时、主/子 Agent 取消、重启恢复、补丁冲突无半写入。

## E2E

Playwright Electron 覆盖新建任务、选择目录、发送、审批、取消、恢复、Changes/Files/Terminal/Trace 标签和 1280×800 视觉回归。

## 通过标准

Core、tools、policy 行覆盖率不低于 80%，分支覆盖率不低于 70%；`pnpm lint/typecheck/test/build` 全部通过。

## 2.0 已执行垂直闭环

Plan 写入阻断、Checkpoint 冲突检测、V1 尾行恢复、Assistant tool-call 落盘、附件上下文、Review 事件和真实 Electron UI 交互均有自动化用例；E2E 直接启动构建后的 Electron，检查任务创建、模式切换和检查器按钮。
